As described in the last step of the [backend's README](/backend/README.md),
once the service account key is created and downloaded, it should be renamed
to `credentials.json` and moved to this directory. 